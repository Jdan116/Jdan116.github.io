<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/style.css">
    <title>Detial</title>
</head>

<body>
    <h1>Laptop specification</h1>
    <% const description = current_element.description %>
    <% const price = current_element.price %>
    <% const id = current_element.id %>

    <form action="/addToCart?id=<%= id %>" method="post">
    <div class="lap_detial">
        <div class="label">Description: <%= description %>
        </div>
        <div class="label">Price: <%= price %> </div>
        <div class="label">Quantity: <input id="quantity_id" type="number" min="1" name="quantity" value="1"></div>
        <div class="label"><img id="prod_image" src="<%= current_element.image %>"></div>
    </div>
        <input type="submit" value="Add to cart">
    </form>
</body>

</html>